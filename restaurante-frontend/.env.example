# URL base de la API backend (Spring Boot). En desarrollo local usa el
# fallback de src/api/axios.js (http://localhost:8080/api/v1); en producción
# define esta variable con el dominio real del backend desplegado.
VITE_API_URL=http://localhost:8080/api/v1
